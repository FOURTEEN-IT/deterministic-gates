# On, not optional: two of the gates used to reach for the project during execution, which only
# fails once a stored entry is reused. Enabling it here means this build -- and CI with it --
# exercises that path on every run instead of leaving it to whichever consumer turns it on first.
org.gradle.configuration-cache=true
