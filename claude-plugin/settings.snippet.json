{
  "_comment": "Example .claude/settings.json wiring for the two hooks in claude-plugin/hooks/, for a project that wants to copy the hook scripts directly instead of installing this whole directory as a Claude Code plugin. Adjust the paths if you place the scripts somewhere other than ci/.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PROJECT_DIR:-.}/ci/session-start.sh\"",
            "timeout": 15,
            "statusMessage": "Reading working state and open decisions"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PROJECT_DIR:-.}/ci/main-branch-rule.sh\"",
            "timeout": 30,
            "statusMessage": "Checking branch/commit rules"
          }
        ]
      }
    ]
  }
}
