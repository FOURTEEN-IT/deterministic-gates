{
  "_comment": "Example .claude/settings.json wiring for hooks/main-branch-rule.sh, for a project that wants to copy the hook script directly instead of installing this directory as a Claude Code plugin. Adjust the path if you place the script somewhere other than ci/.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PROJECT_DIR:-.}/ci/main-branch-rule.sh\"",
            "timeout": 30,
            "statusMessage": "Checking branch/commit rules"
          }
        ]
      }
    ]
  }
}
